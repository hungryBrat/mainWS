1. skill-vagabond
Shadow Collapse Validation
After Shadow produces its cycle-2 synthesis, run a lightweight collapse check: apply the synthesis as a new topic through 3 fast lens passes (Mathematics, Physics, Philosophy). If the mini-lattice produces the same structural nodes as the original topic → the dialectic achieved clever reframing, not a new paradigm. Re-trigger Shadow with explicit instruction to go harder. Only accept output when the synthesis generates new convergence nodes. Creates a mathematically defined acceptance criterion.

